apiVersion: v2
name: agent
description: The Agent service, a hardened non-root FastAPI application
type: application

version: 0.1.0
# The fallback for an empty image.tag.
appVersion: "0.1.0"

# topologySpreadConstraints and seccompProfile.
kubeVersion: ">=1.27.0-0"

keywords:
  - agent
  - fastapi
home: https://example.invalid/agent

apiVersion: v2
name: qdrant
description: The vector DB, the official Qdrant chart with this project's defaults
type: application

version: 0.1.0
appVersion: "1.19.0"

kubeVersion: ">=1.27.0-0"

# A dependency and not a fork: a version bump is this line plus `helm dependency
# update`, and the overrides in values.yaml stay visible.
dependencies:
  - name: qdrant
    version: "1.19.0"
    repository: https://qdrant.github.io/qdrant-helm

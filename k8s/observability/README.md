# Observability

This chart packages the official `kube-prometheus-stack`, `loki`, and
`promtail` charts as dependencies, with the Agent ServiceMonitor and Grafana
dashboard. Installation and usage are documented in the [repository
README](../../README.md#monitoring-and-logs).

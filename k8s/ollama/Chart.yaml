apiVersion: v2
name: ollama
description: The inference server the Agent service talks to
type: application

version: 0.1.0
# The fallback for an empty image.tag.
appVersion: "0.32.15"

# Matches the floor the agent chart sets.
kubeVersion: ">=1.27.0-0"

keywords:
  - ollama
  - llm
  - inference
home: https://ollama.com
sources:
  - https://github.com/ollama/ollama

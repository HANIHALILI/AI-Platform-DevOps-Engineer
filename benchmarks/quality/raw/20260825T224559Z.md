# Quality run

- Model: `agent-llm`
- Cases: `benchmarks/quality/cases.tsv`

## readiness_vs_liveness — PASS

A **readiness probe** checks if a container is ready to receive traffic; if it fails, the pod is not marked as "ready" and is excluded from service routing. A **liveness probe** determines if a container is still functioning; if it fails, the pod is restarted to recover.

## rag_grounding — FAIL (missing: retriev source unsupported not know)



## context_window — FAIL (missing: memory token)

An LLM service needs a context-window limit to manage computational resources and prevent performance degradation from processing excessive input. It also ensures accuracy by avoiding information overload and maintaining focus on relevant data.

## quantization_tradeoff — FAIL (missing: memory speed precision)



